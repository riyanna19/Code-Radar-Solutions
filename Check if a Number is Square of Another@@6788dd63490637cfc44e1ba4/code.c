#include <stdio.h>
#include <maths.h>

int main(){
    int a;
    int b;
    scanf("%d %d", &a, &b);
    if(pow(a==b*2)){
        printf("Yes");
    }else{
        printf("No");
    }
    return 0;

}